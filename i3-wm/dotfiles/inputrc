## .inputrc based on https://github.com/webpro/dotfiles/blob/master/runcom/.inputrc
set completion-ignore-case on
set show-all-if-ambiguous on
set mark-symlinked-directories on
set page-completions off

# Ask to show more than 100 possible completions for a word
set completion-query-items 100

# Show extra file information when completing, link `ls -F` does
set visible-stats on

# Intelligent autocompleting, when the cursor is inside a word, he autocomplet it after the word
set skip-completed-text on

# Allow UTF-8 input and output
set input-meta on
set output-meta on
set convert-meta off

# Flip through autocompletion matches with Shift-Tab
"\e[Z": menu-complete

# Filtered history search
"\e[A": history-search-backward
"\e[B": history-search-forward
